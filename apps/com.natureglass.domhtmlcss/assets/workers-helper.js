// Loaded by workers-tier0.html's worker via importScripts(). The
// importScripts call eval's this file in the worker's global scope,
// so the function lands on `self`. Used by the fixture to verify
// importScripts actually loaded + the loaded code can be called.
self.__importedHelperVersion = 'v1';
self.__importedAdd = function (a, b) { return a + b; };
